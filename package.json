{
  "name": "sochle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.21.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present run build",
    "dev": "pnpm --parallel --filter @sochle/web --filter @sochle/extension run dev",
    "e2e": "pnpm build && pnpm --filter @sochle/extension exec wxt clean && WXT_SOCHLE_API_ORIGIN=http://127.0.0.1:3101 pnpm --filter @sochle/extension build && playwright test",
    "e2e:ui": "pnpm build && pnpm --filter @sochle/extension exec wxt clean && WXT_SOCHLE_API_ORIGIN=http://127.0.0.1:3101 pnpm --filter @sochle/extension build && playwright test --ui",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:extension-security": "pnpm --filter @sochle/extension exec wxt clean && WXT_SOCHLE_API_ORIGIN=http://localhost:3000 pnpm --filter @sochle/extension build && node scripts/verify-extension-bundle.mjs apps/extension/.output/chrome-mv3",
    "test:integration": "vitest run --project integration",
    "test:unit": "vitest run --project unit",
    "test:watch": "vitest",
    "typecheck": "pnpm --recursive --if-present run typecheck && tsc -p tsconfig.tests.json"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@eslint/js": "^9.39.2",
    "@playwright/test": "^1.62.1",
    "@sochle/contracts": "workspace:*",
    "@sochle/db": "workspace:*",
    "@sochle/domain": "workspace:*",
    "@types/node": "^24.10.1",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^9.39.2",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.49.0",
    "vitest": "^4.0.16"
  }
}
